# Hub Prioritization Framework - runtime requirements
# ===================================================
# The authoritative dependency list lives in pyproject.toml.
# Install with:  pip install -e .            (runtime)
#                pip install -e .[dev]       (runtime + tests/lint)
#                pip install -e .[ahp,viz]   (optional extras)
#
# This file mirrors the runtime set for tools that still read requirements.txt.

pandas>=2.2
numpy>=1.26
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
pyogrio>=0.9
rtree>=1.1
# The code uses the h3 v4 API (latlng_to_cell, cell_to_boundary, grid_disk,
# h3shape_to_cells_experimental for the base layer).
h3>=4.1,<5
openpyxl>=3.1
pyyaml>=6.0
# Parquet read/write for the H3 base layer (data/reference/h3_base.parquet).
pyarrow>=15

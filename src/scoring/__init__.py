"""
Optional scoring extras.

The production scoring lives in :mod:`src.pipeline.scoring`. This package keeps the
two optional analyses that complement it:

- :mod:`src.scoring.ahp` — expert-driven weights (Analytic Hierarchy Process)
- :mod:`src.scoring.mc_distribution` — Monte Carlo distribution / robustness reporting
- :mod:`src.scoring.normalization` — shared 1-10 normalisation helpers
"""
